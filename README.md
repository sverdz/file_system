# File Inventory Tool

Інструмент для інвентаризації, класифікації та організації файлів з підтримкою OCR, виявлення дублікатів та автоматичного перейменування.

## 🚀 Швидкий старт (Windows)

### Вимоги
- Python 3.8 або новіший
- Tesseract OCR (опціонально, для розпізнавання тексту з зображень)

### Встановлення та запуск

1. **Перейдіть до директорії проекту:**
   ```cmd
   cd /d "C:\Users\dsver\OneDrive\Documents\GitHub\file_system"
   ```

2. **Запустіть програму:**
   ```cmd
   run.bat
   ```

   Скрипт автоматично:
   - Створить віртуальне середовище Python (.venv)
   - Встановить всі необхідні залежності
   - Запустить програму

### Альтернативний спосіб

**Двічі клікніть по файлу `run.bat` в провіднику Windows** - це автоматично запустить програму з правильної директорії.

## 📋 Можливості

- **Сканування файлів**: Рекурсивне сканування директорій
- **Виявлення дублікатів**: Точне порівняння за хешем SHA-256
- **OCR**: Розпізнавання тексту з PDF та зображень (українська/англійська)
- **Класифікація**: Автоматична категоризація документів
- **Перейменування**: Створення структурованих назв файлів
- **Експорт**: Звіти в форматі Excel (XLSX)

## 🔧 Налаштування

При першому запуску програма запропонує налаштувати:
- **Директорію для аналізу**: Шлях до папки з файлами
- **Мову OCR**: `ukr+eng`, `eng`, або `off`
- **LLM**: Увімкнути/вимкнути інтеграцію з мовними моделями

## 📖 Основні команди

Після запуску виберіть опцію з меню:

1. **Швидкий аналіз (dry-run)** - Показує що буде зроблено без застосування змін
2. **Застосувати перейменування (commit)** - Виконує фактичні зміни
3. **Переглянути підсумок** - Показує результати останнього запуску
4. **Налаштування** - Змінити конфігурацію
5. **Сортування та подання** - Організація файлів по категоріях

## 🐛 Вирішення проблем

### Помилка: "ModuleNotFoundError: No module named 'app'"

**Причина**: Скрипт запущено з неправильної директорії.

**Рішення**:
```cmd
cd /d "C:\Users\dsver\OneDrive\Documents\GitHub\file_system"
run.bat
```

### Помилка: "python is not recognized"

**Причина**: Python не встановлений або не доданий до PATH.

**Рішення**:
1. Завантажте Python з https://www.python.org/downloads/
2. При встановленні поставте галочку "Add Python to PATH"
3. Перезапустіть командний рядок

### Помилка встановлення залежностей

**Рішення**:
```cmd
cd /d "C:\Users\dsver\OneDrive\Documents\GitHub\file_system"
.venv\Scripts\activate
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

## 📁 Структура проекту

```
file_system/
├── app/                    # Основний код програми
│   ├── main.py            # Точка входу
│   ├── config.py          # Конфігурація
│   ├── scan.py            # Сканування файлів
│   ├── dedup.py           # Виявлення дублікатів
│   ├── extract.py         # Вилучення тексту
│   ├── classify.py        # Класифікація
│   ├── rename.py          # Перейменування
│   └── ...
├── runs/                  # Результати запусків
├── .venv/                 # Віртуальне середовище Python
├── requirements.txt       # Залежності
├── run.bat               # Скрипт запуску для Windows
└── README.md             # Цей файл
```

## 🔐 Вимоги OCR

Для роботи OCR потрібен Tesseract:

1. Завантажте з: https://github.com/UB-Mannheim/tesseract/wiki
2. Встановіть з підтримкою української мови
3. Додайте шлях до Tesseract в PATH

## 📝 Ліцензія

Цей проект створено для організації та управління файлами.

## 📞 Підтримка

При виникненні проблем:
1. Перевірте що Python встановлено: `python --version`
2. Перевірте що ви в правильній директорії: `dir` (повинні бачити `app\main.py`)
3. Перезапустіть `run.bat`

---

**Примітка**: Завжди запускайте `run.bat` з директорії проекту або двічі клікайте по ньому в провіднику.
